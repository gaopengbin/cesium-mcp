## What

Brief description of the changes.

## Why

Why is this change needed?

## Checklist

- [ ] Tests pass (`npx vitest run`)
- [ ] Build succeeds (`npm run build`)
- [ ] Changeset added (`npx changeset`) — if user-facing change
