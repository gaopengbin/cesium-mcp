# Cesium × cesium-mcp × Jev 场景实验

本实验让用户在东京丸之内的真实建筑区自由点选起终点，观察橙色 RobotExpressive 人形角色怎样前往目标。默认显示由 PLATEAU 建筑几何生成的本地无纹理白模，也可切换在线纹理建筑。几何算法提供路线候选，Jev 选择路线并持续决定短段动作，cesium-mcp Bridge SDK 传递选择与运动意图，Rapier 和本地控制器执行。

开发分支：`codex/cesium-jev-demo`。本地新入口：[东京建筑区](http://127.0.0.1:4192/?planner=jev&scene=city)。原 `4186` 演示保留，当前版本尚未部署公网。

## 本地运行

需要 Node.js 22 或更新版本。仅安装实验自己的依赖，在仓库根目录运行：

```powershell
rtk npm --prefix experiments/embodied-world-lab ci
```

在实验目录被 Git 忽略的 `.env.local` 中配置 `TYPESAFE_API_KEY`，然后启动：

```powershell
rtk npm --prefix experiments/embodied-world-lab run dev
```

页面默认选择 `city` 自由任务场景，并载入约 1.5 公里的任务；用户仍可自行点选 A/B。等待场景、角色和碰撞数据就绪后再开始，加载页面不会自动导航。本地白模、导航与碰撞共用随实验保存的 `src/assets/tokyo-buildings.bin`，首次运行不必重新下载生成，也不请求在线建筑纹理瓦片。底图和模型接口仍需要联网。Jev 密钥由本地 Vite 服务端读取，浏览器通过同源 `/api/jev/route` 请求路线选择、通过 `/api/jev/plan` 请求短段动作；不要将 Jev 密钥放入前端配置或 Git。模型调用产生 API 用量，纯静态部署不包含服务端代理。

碰撞数据的准备脚本为 [`scripts/prepare-city-colliders.mjs`](scripts/prepare-city-colliders.mjs)。可先运行不联网、不改输出的坐标变换自测：

```powershell
rtk npm --prefix experiments/embodied-world-lab run prepare:city -- --self-test
```

需要重新生成数据时，运行不带 `--self-test` 的 `prepare:city`。该操作会从固定 PLATEAU 源下载选定叶节点、检查坐标与射线，并覆盖 `src/assets/tokyo-colliders.json`；不是每次启动的必需步骤。

JSON 更新后，再从仓库根目录运行下面的离线转换，生成运行时共用的二进制网格及来源元数据；此步骤不会请求网络，也不会简化或删减建筑三角形：

```powershell
rtk proxy node experiments/embodied-world-lab/scripts/prepare-city-binary.mjs
```

## 建筑显示方式

页面的建筑显示选项对应 URL 参数 `buildings`，切换后重新载入所选数据源：

| 参数 | 显示内容 | 导航 |
| --- | --- | --- |
| `white`（默认） | 本地 PLATEAU 建筑无纹理白模、Esri 浅灰底图 | 可选点并运行 Jev；显示、导航和碰撞共用几何 |
| `plateau` | 在线 PLATEAU LOD2 纹理建筑、Esri 影像 | 可选点并运行 Jev；沿用同源本地建筑碰撞网格 |
| `google` | Google Photorealistic 3D Tiles | **仅浏览，禁用人物导航**；没有对应的导航与碰撞数据 |

**默认白模不是 Google 数据，也不是全球建筑服务。** 它保留东京已准备区域的真实 PLATEAU 建筑形状，省去纹理与远程瓦片层级切换。Google 提供的是实景纹理网格；Cesium OSM Buildings 是另一项全球建筑服务，当前未接入。

Google 浏览模式需要在本地 `.env.local` 配置以下任意一项，并重启 Vite：

```dotenv
# Google Maps Platform：已启用 Map Tiles API 的受限制浏览器 key
VITE_GOOGLE_MAPS_API_KEY=
# 或：已启用 Google Photorealistic 3D Tiles 资产的 Cesium ion token
VITE_CESIUM_ION_TOKEN=
```

这些 `VITE_` 变量会进入浏览器，须使用按来源域名和所需 API/资产限制权限的凭据；它们不同于仅由服务端持有的 `TYPESAFE_API_KEY`。不要把真实值提交 Git。Google 直连需要项目启用计费；Ion 路径使用自己的账户及资产权限，不依赖 Cesium 库内置演示 token。配置要求见 [Cesium 官方教程](https://cesium.com/learn/cesiumjs-learn/cesiumjs-photorealistic-3d-tiles/)及 [Google Map Tiles API 使用与计费](https://developers.google.com/maps/documentation/tile/usage-and-billing)。

当前环境未配置这两项凭据，**Google 在线加载尚未验证**。未配置时选择项显示“需配置”；直接打开 `buildings=google` 会明确提示并显示本地白模，不伪称已加载 Google。配置 Google 后只开放浏览，不把东京 PLATEAU 碰撞网格套用到另一份实景数据上。Google 标识与数据署名须保持可见。

## 自由选择任务

1. 在街区全景中平移、缩放地图，依次点击起点 **A** 和终点 **B**。
2. 查看选点结果与可行路线，再开始 Jev 导航。可以单独重选 A 或 B，也可以交换起终点。
3. 运行时可切换跟随与街区全景。全景保留地图原生平移、缩放操作，切换镜头不会控制人物朝向。
4. 使用“回到起点”重新体验当前点对，或载入单楼、穿越街区、1.5 公里示例。
5. 点击“生成任务链接”，复制显示的地址，让新窗口恢复相同的起终点。链接字段可手动选中复制，不依赖内置浏览器的自动剪贴板支持。

屋顶、建筑附近网格净距不足或超出覆盖范围的点会明确拒绝，**不会自动吸附到另一处位置**。两个点各自有效，但当前导航网格未找到连接路线时，保留坐标并禁止开始；这不等于证明现实中没有道路。选点有效和路线存在并不等于已经证明 Jev 能完成执行。

任务链接使用 `from=经度,纬度` 与 `to=经度,纬度`，输出最多七位小数，城市高度固定为 38 米。两项必须成对出现；不完整或非法经纬度不能部分生效。其他查询参数与 URL 片段会保留。链接重现点对，不重现或伪造之前的模型行动记录。

## 路线由谁决定

1. 本地算法将真实建筑网格保守投影到 **1 米导航网格**，按不变的 **2 米安全净距**搜索可行路线。候选类型包括直接前往的 `direct`、局部左绕或右绕的 `left` / `right`，以及在导航覆盖网格内通过全图 A* 搜索得到的 `detour`。`detour` 可以跨越起终点直线的两侧，不要求整条路径固定在某一侧。
2. Bridge 的 `readNavigationOptions` 提供当前可用候选及结构化摘要。Jev API 从候选中选择，或返回 `hold`，结果通过 `commitNavigationRoute` 提交并校验。不会把当前不存在的路线当作可用选项。
3. 路线接受后，本地路线跟随器提供下一个拐点。Jev 继续依据距离、朝向与局部状态选择短段 `advance`、转向或停留；本地控制器执行并检查安全。

**候选坐标与 A* 搜索由程序计算，Jev 负责候选选择和短段行动决策。** 建筑信息来自已加载的城市网格，模型接收程序组织的结构化数据；没有从截图识别建筑，也没有让模型凭空生成整条路径。

## 连续行动怎样调整

旧版本的一段动作约两秒，到期后才等待下一次模型结果，因此会出现“走一段、停下来、再走”的节奏。当前循环在有效动作继续执行时提前请求下一段计划：

- 本实验配置提前最多 **3,000 毫秒**规划，提前窗口不超过当前动作时长的一半。
- 有效动作在等待响应期间继续执行；单次 `advance` 最长 **6 秒**。
- 转向已经对齐目标方向时，可提前结束转向，不必等满整段时长。
- 本地 **20 Hz** 安全循环继续检查风险、碰撞、计划有效性和停止请求，必要时干预动作。

这些调整消除了“必须等动作到期才请求”的安排。转向响应已调整，俯视相机也与人物朝向控制分离。原地转向、等待下一计划、模型超时或安全干预仍可能使角色停止平移，不能宣称整轮零停顿。

## 演示速度、人物与镜头

城市导航提供 **3 / 8 / 12 米/秒**三个速度上限，默认 **8 米/秒**。本地控制会在接近航点时按距离降速，同时按朝向误差限速：误差不超过 0.2 弧度时保留距离限速结果，0.2–0.8 弧度间平滑降速，达到 0.8 弧度时停止平移、继续对齐方向。选择的速度不是整轮平均速度，也不是对真实行人能力的模拟。Walking / Running 动画按实际速度切换，不通过冲刺输入把物理速度翻倍。

人物外观使用 Cesium 模型 `scale: 2`、`minimumPixelSize: 110`、`maximumScale: 5` 提高辨识度。它们只作用于显示，物理胶囊仍按原来的 **1.8 米身高、0.3 米半径**配置；放大的模型轮廓不能用于判断精确碰撞净距。

跟随镜头与街区地图相机独立，提供 **35 / 70 / 120 米**相对角色高度，默认 **70 米**。相机从角色上方的视线起点检查建筑遮挡，优先沿用可用侧向机位，再寻找同高度的其他方向；必要时升高到所选高度的 1.5 或 2 倍，最高 180 米。只有侧向机位均不可用时才采用近顶视保底。平滑位置若被遮挡，会直接使用已选的无遮挡机位；全部候选都被拒绝时，保底只保证有效的高位姿态，不保证无遮挡。相机瞄准点稍向人物前方偏移，以便窄面板中仍能看清人物；这些镜头调整不会改变人物朝向，也不向 Jev 提供截图。

## 城市场景与数据边界

默认城市位于东京站西侧的丸之内街区，建筑几何来自 **国土交通省 Project PLATEAU 千代田区 2025 年度带纹理 LOD2 3D Tiles**；默认白模去掉纹理，不替换建筑形状。已纳入版本控制的 [`src/assets/tokyo-colliders.json`](src/assets/tokyo-colliders.json) 保存源地址、瓦片哈希、建筑标识、坐标编码与验证记录；源数据生成过程见[准备脚本](scripts/prepare-city-colliders.mjs)。这是真实城市建筑数据，不是随机生成的楼群。

渲染建筑不会自动成为 Rapier 碰撞体。本实验已从同源 **64 个最高细节源瓦片、879 个唯一源建筑标识**提取并保留 **317,525 个三角形**。原始碰撞 JSON 为 **29,790,254 字节**；当前运行时二进制网格为 **5,861,120 字节**，约减少 **80.3%**，包含文件头与来源元数据。这是文件体积对比，不是加载时间或帧率测量。白模显示、导航和碰撞共用该二进制网格，原 JSON 保留为可追溯来源与审计输入。

[`scripts/prepare-city-binary.mjs`](scripts/prepare-city-binary.mjs) 仅合并完全相同的源顶点，将 **952,575** 个顶点记录去重为 **170,759** 个，再编码为 Float32 局部 ECEF 偏移与 Uint32 索引；三角形数量、顺序及绕序保留。相对于原 JSON，Float32 最大单坐标误差约 **0.00006055 米**。二进制哈希、源 JSON 哈希与处理说明见 [`src/assets/tokyo-buildings.meta.json`](src/assets/tokyo-buildings.meta.json)。没有使用 OSM footprint 拉伸、几何简化或建筑高程偏移。源转换的坐标变换、包围区域和射线检查已有记录；路线几何检查与模型实际执行轨迹的审计分别记录。

碰撞覆盖范围为 **`[139.758, 35.676, 139.7678, 35.692]`**，约东西 0.89 公里、南北 1.78 公里，总计约 **1.6 平方公里**。与该范围相交的叶节点保留完整几何，范围之外不保证覆盖完整。源碰撞数据仅包含建筑，不将建筑底面当成真实道路表面。

城市地面明确采用 **38 米恒定椭球高的全区地面网格近似**，碰撞地面与显示地面使用同一高度基准，仍不是实测道路地形。东京附近 ArcGIS 高程样本约为 2.9 米，但服务声明使用正高；其基准与建筑数据存在差异，不能将该数值静默当作椭球高。建筑 mesh 不做高程偏移。角色按此地面放置，这不代表真实道路坡度或测量精度。

默认白模用本地 Cesium 几何显示，不请求 PLATEAU 纹理瓦片，也没有该纹理瓦片的 LOD 加载与逐出过程。选择 `plateau` 时，在线建筑仍使用 `maximumScreenSpaceError: 16`，关闭移动期间的请求裁剪和 foveated 加载延迟，缓存预算为 **1 GiB + 256 MiB 溢出额度**。此前纹理版本调试中 512 MiB 缓存不足，提高预算后仍观察到瓦片卸载；下方历史结果只适用于当时纹理版本，不能作为白模性能结果，也不代表手机内存占用或任意设备性能保证。

白模底图使用 **Esri World Light Gray Base**，保留服务署名 `Esri, HERE, Garmin, (c) OpenStreetMap contributors, and the GIS user community`。该服务元数据列出的最高层级不等于每处实际覆盖；东京范围的瓦片可用性元数据显示 16 级有数据、17 级及以上样本无数据，因此当前底图明确限制 **`maximumLevel: 16`**。这是简洁背景，近距离道路细节有限；[官方服务元数据](https://server.arcgisonline.com/ArcGIS/rest/services/Canvas/World_Light_Gray_Base/MapServer?f=pjson)提供来源与服务说明。

| 数据或组件 | 来源及作用 |
| --- | --- |
| 默认城市白模与可选纹理建筑 | PLATEAU / 国土交通省，千代田区 2025；白模去纹理并使用同源几何，保留独立处理说明 |
| 白模底图 | Esri World Light Gray Base；东京限制至 16 级，保留 Esri、HERE、Garmin、OpenStreetMap 与 GIS 社区署名 |
| 可选实景浏览 | Google Photorealistic 3D Tiles；需配置访问凭据，尚未在线验证，不开放人物导航 |
| 纹理场景底图影像 | Esri World Imagery，仅用于显示，不传给 Jev 作为视觉证据 |
| 旧山地场景高程 | ArcGIS World Elevation，用于旧山地预设；不作为城市恒高地面网格的测量来源 |
| 当前城市角色 | Three.js 官方示例中的 RobotExpressive：Tomás Laulhé（Quaternius）建模、Don McCurdy 修改，CC0；使用橙色材质和具名步行动画，见 [模型许可](src/assets/RobotExpressive.LICENSE.md) |
| 保留的 Fox 资产 | Khronos glTF Sample Assets 的未修改资产；作者与许可见 [第三方归属](public/THIRD_PARTY_NOTICES.md) 和 [Fox 许可](src/assets/Fox.LICENSE.md) |
| 任务与控制 | 本实验添加的角色行为、目标、局部观测、安全控制及碰撞处理 |
| 决策服务 | TypeSafe 的 Jev API；本实验不是其官方产品 |

PLATEAU 数据需保留来源并标注实验所做处理。其[一般使用政策](https://www.mlit.go.jp/plateau/site-policy/)已核对；来源记录说明，数据集专属页面的条款尚未独立核验，不能将一般政策表述为已确认不存在额外权利说明。人物资产和各地图数据继续遵守各自条款，不因仓库许可证而重新授权。

## 观测、控制与其他场景

Bridge SDK 使用 `readNavigationOptions` / `commitNavigationRoute` 连接路线选择，使用 `observeWorld` / `commitMotionIntent` / `stopEmbodied` 连接短段观测、运动意图与停止。当前页面直接调用 `CesiumBridge` 执行通道，**尚未串联独立 MCP stdio/HTTP 客户端与 runtime 服务**。

Jev 接收候选路线摘要，以及目标距离、方位差、接地状态、速度和候选方向等结构化观测，返回类型化选择。请求使用 `jev-latest`，实际模型名以当次记录为准。角色采用步行胶囊控制与动画，本实验不实现车辆自动驾驶、真实机器人硬件控制或视觉识别。本地寻路、安全干预与路线跟随不能记为模型推理结果。

| `scene` | 配置 |
| --- | --- |
| `city` | 默认：东京丸之内约 1.5 公里任务，可自由点选 A/B，也提供单楼与穿越街区示例 |
| `inspection` | 旧山坡巡检起点、目标与人工风险布局 |
| `wide-detour` | 旧山地场景，扩大前方风险圆 |
| `near-risk` | 旧山地场景，将风险圆移近起点 |
| `random` | 在旧山地场景中按种子改变目标与人工风险圆 |

例如 [山坡巡检](http://127.0.0.1:4192/?planner=jev&scene=inspection&seed=260921) 与 [随机配置](http://127.0.0.1:4192/?planner=jev&scene=random&seed=846641)。随机化的是实验配置，不是东京建筑或真实地形；同种子重现布局，不保证同一行动序列。

历史滑块查看已记录的位置与观测，不调用模型，也不移动当前活角色；它不是重新执行物理模拟。运行记录通过“查看记录”打开只读 JSON，可全选后手动复制，无需浏览器下载或自动剪贴板支持。

## 2026-09-22 白模与界面验收

界面按 Google Workspace 的白底蓝色工作台风格重做，使用系统字体；主区域保留 A/B、选点、速度、镜头和固定开始/停止操作。场景设置、预设路线、决策详情、日志回放和其他实验默认折叠。第一轮白模验收检查了 1280、1024 与 390 像素宽度，无横向溢出；PLATEAU 实景和白模双向切换保留原始起终点，Google 无凭据时禁用选项。下拉框等控件随后替换为组件库，见下方独立记录。

白模页面实际只请求一次 **5,861,120 字节**建筑二进制，没有请求旧碰撞 JSON 或 PLATEAU 远程建筑纹理。类型检查、生产构建及 **22 个测试文件、216 项测试**通过；构建产物不包含旧 29.79 MB JSON。

相同用户点对 `139.761788,35.676708` → `139.766056,35.683870` 在白模模式下完成真实 **jev-1.13.0** 导航：12 米/秒上限，31 次响应全部接受，实际行程 **942.707 米**，位置记录跨度 **97.611 秒**，最终距目标 **2.426 米**并停止。导航网格与碰撞几何没有因显示改版而放宽安全距离。见[完整记录](../../artifacts/jev-white-user-route-run.json)及[到达画面](../../artifacts/jev-white-arrived.png)。

[原始建筑网格审计](../../artifacts/jev-white-user-route-audit.json)将 1,628 个位置记录插值为 4,152 个样本，记录轨迹与建筑三角形 **0 次相交**，保守身体表面净距下界 **2.121 米**。最大记录间隔 69.9 毫秒。结论仅覆盖这次记录的插值轨迹，未做帧率基准测试；Google 在线加载仍未验证。

## 2026-09-22 组件库控件验收

控制台使用 **Web Awesome 3.13.0**，按需注册 Select、Option、Button、Details、Input、Textarea、Slider 和 Dialog，保留白蓝配色及原有布局。四个下拉菜单使用组件弹层，不再显示操作系统选择菜单；焦点管理、键盘选择、弹层定位和弹窗由组件库处理。仅导入主题变量，没有引入库的全局原生元素重置；内置图标由本地包提供。首次注册前隐藏控件，避免选项文本与未打开的弹窗短暂裸露。

浏览器实际检查了 1280、1024 和 390 像素宽度，无横向溢出。速度与镜头选项改变了执行器参数；方向键、Enter、Escape、点击外部关闭正常。动态场景选项更新说明，Google 未配置选项保持禁用，任务链接可完整选中。无效种子后点击“随机一个”可一次应用新场景，校验前等待组件内部输入更新。组件表单的“查看场景”提交成功；记录弹窗在桌面和手机尺寸均可用，全选覆盖完整文本，Escape 关闭后焦点回到“查看记录”。

短时真实 Jev 导航生成 **54 个回放位置**，停止后组件滑块从第 1 帧切到第 54 帧并成功退出回放；本轮用于控件联动验收，没有重新声称完成整条路线。页面错误日志为空。类型检查、生产构建及 **22 个文件、216 项测试**通过。截图：[桌面菜单](../../artifacts/jev-components-desktop.png)、[1024 像素布局](../../artifacts/jev-components-1024.png)、[手机菜单](../../artifacts/jev-components-mobile.png)。这些截图保存在本地忽略目录，不随 Git 分发。

## 历史记录：1 米导航网格修复（纹理版本）

以下路线、模型运行、测试与截图记录来自此次白模和界面调整之前的纹理版本。保留为可复查的历史证据，不作为新显示方式、Google 接入或新界面的验收结果。

用户反馈的起点 `139.761788,35.676708` 与终点 `139.766056,35.683870` 在旧 2 米网格中都有效，却被分到不同连通区，导致直接拒绝搜索。保守投影与整格膨胀的离散误差封闭了可通行出口。改为 **1 米网格、仍保留 2 米安全净距**后，原样坐标得到 **951.281 米、11 个拐点**的 `detour`；候选每段均通过独立原始建筑三角网求交检查。对比结果、网格哈希与候选坐标见 [网格回归记录](../../artifacts/jev-user-route-grid-fix.json)。

当前网格为 **818 × 1706 = 1,395,508 格**，容量上限为 200 万格；一次 Node 测量的初始化约 994 毫秒、此路线规划约 615 毫秒，不代表任意设备响应时间。**19 个测试文件、205 项测试通过**，类型检查和构建通过。UI 将搜索失败表述为当前网格尚未找到路线，保留 A/B 并说明 Jev 尚未参与；依次选点尚未完成时不提前显示否定结论。

相同点对已完成真实 **`jev-1.13.0`、12 米/秒上限**执行：31 次响应全部接受，实际行程 **942.538 米**，记录用时 **98.792 秒**，最终距终点 **2.480 米**并停止。本地安全状态为 `clear`，记录 **1,668 个位置样本**。见 [运行记录](../../artifacts/jev-user-route-fixed-run.json)、[到达截图](../../artifacts/jev-user-route-fixed-arrived.png)。候选路线长度和实际行程分别统计，不将路径生成成功当作执行成功。

[实际轨迹网格审计](../../artifacts/jev-user-route-fixed-mesh-audit.json)将这些记录点插值为 4,670 个间距不超过 0.25 米的样本，与 317,525 个建筑三角形检查得到 **0 次轨迹相交**。中心最小采样距离为 3.368 米，按 1.8 米身高、0.3 米半径身体外包球估计的保守体表净距下界为 2.103 米。最大记录间隔 101.1 毫秒；该结论覆盖记录点之间的插值轨迹，不证明未记录的细微运动或任意其他路线安全。

## 历史记录：扩区后的 2 米导航网格

以下为改用 1 米网格之前的扩区与提速结果；当时 **19 个测试文件、204 项测试通过**，类型检查和构建通过。默认长途任务生成 **1,488.63 米 / 1,582.32 米**两条候选，候选几何路径检查未与建筑网格相交；候选检查与下方真实执行记录分别统计。当前网格重新规划的候选可能不同，这些运行记录不代表当前任意任务的结果。

相同旧点对在扩区后已生成约 **270 米左侧 / 481 米右侧**候选，不能沿用旧数据的路线长度和成功记录。新数据上的短路线已完成真实 **`jev-1.13.0`、8 米/秒**运行：14 次决策，实际行程 **264.33 米**，耗时 **40.03 秒**，记录 **686 个位置样本**，最终距目标 **2.380 米**。运行中测得速度达到 8 米/秒，结束时本地安全状态为 `clear`；记录见 [jev-city-fast-8ms-run.json](../../artifacts/jev-city-fast-8ms-run.json)。

短路线的[独立建筑网格审计](../../artifacts/jev-city-fast-8ms-mesh-audit.json)已完成：686 帧插值为 1,474 个采样位置，记录轨迹与楼体 **0 次相交**；中心到 mesh 最小采样距离 **5.631 米**，XY 投影距离 **5.606 米**，按 1.8 米身高、0.3 米半径身体外包球计算的保守体表净距下界为 **4.366 米**。这些距离分别描述不同几何量，结论仅覆盖本次记录的插值轨迹和指定身体近似，不是任意任务的安全保证。

约 1.5 公里任务已完成真实 **`jev-1.13.0`、12 米/秒**运行。Jev 选择约 1,489 米的右绕候选，实际行程 **1,470.442 米**，耗时 **167.45 秒（约 2 分 47 秒）**，最终距目标 **2.445 米**并停车。共收到 **48 次响应、45 次接受**，记录 **2,327 个位置样本**，沿途实测速度达到 12 米/秒。证据：[完整运行记录](../../artifacts/jev-city-long-12ms-run.json)、[到达截图](../../artifacts/jev-city-long-arrived.png)、[沿途跟随截图](../../artifacts/jev-city-long-follow.png)。

长途的[独立建筑网格审计](../../artifacts/jev-city-long-12ms-mesh-audit.json)已完成：将 2,327 个记录点插值为 **7,394 个间距不超过 0.25 米的扫掠样本**，对 **317,525 个建筑三角形**检查，记录的插值轨迹与楼体 **0 次相交**。中心到 mesh 最小采样距离为 **5.593 米**，XY 投影距离为 **5.474 米**；按 1.8 米身高、0.3 米半径身体外包球计算，保守体表净距下界为 **4.290 米**。相邻原始记录的最大位移为 **0.9124 米**，最大时间间隔为 **1.0328 秒**，其中 **30 个间隔超过 1 秒**。这是对该次记录点之间插值轨迹的审计，不能覆盖记录之间未观测的微小曲线运动，也不能证明全程 60 FPS 或任意任务安全。

长途运行中，**35 / 70 / 120 米**相机档位操作已通过。连续性统计记录了 **34.144 秒未观察区间**，模型计划续接统计中的最大静止时间为 **250 毫秒**；后者不能代表整轮所有卡顿，也不能据此宣称稳定帧率或零卡顿。

[渲染探针](../../artifacts/jev-city-long-render-probe.json)覆盖其中 **15 秒、718 帧**：可见瓦片数为 **11–17**，可见瓦片为零的帧数为 **0**，失败数为 **0**，期间仍发生 **31 次卸载**。运行结束时累计加载 **1,035** 次、卸载 **977** 次，驻留 **58** 个瓦片，瓦片内存约 **1.183 GB**，失败数为 **0**。这些数字反映 LOD 与缓存的动态复用和逐出，探针也只覆盖所采样的时段；不能写成全程不闪烁、零重载或零卸载。

当前画面已看到人物和两侧建筑。默认 70 米高位跟随测试中，相机约为 **109 米椭球高、36° 俯角**；这是该机位的观察记录，不保证整个街区的任意位置均无遮挡。

## 历史记录：旧范围、2.8 米/秒

以下结果来自扩区和提速前的版本，碰撞数据为 16 个源瓦片、132 个建筑标识，移动速度为 2.8 米/秒；不作为当前 64 瓦片版本或 8 / 12 米/秒配置的完成证据。该轮通过地图实际点选任务，并完成真实 Jev 执行：

- 起点：`139.7637391,35.6804444`。
- 终点：`139.763739,35.6825385`。
- 生成的 `detour` 候选约 **290.99 米、8 个拐点**。

[恢复这组起终点](http://127.0.0.1:4192/?planner=jev&scene=city&from=139.7637391,35.6804444&to=139.763739,35.6825385)。链接只恢复坐标，当前代码与数据不会恢复旧模型结果。旧候选路线长度与实际行程分别记录：

| 旧版执行 | 当时实测结果 |
| --- | --- |
| 实际模型 | `jev-1.13.0` |
| 模型响应 | 34 次，33 次接受，1 次过期丢弃 |
| 实际行程 / 最终距目标 | **286.55 米 / 2.446 米**，到达后停车 |
| 整轮时间 / 位置记录 | **108.3 秒 / 1,880 个密集位置样本** |
| 当前视野建筑加载 | 146 个瓦片，0 个失败 |

独立审计在记录位置之间插值并对真实建筑三角形检查，得到 **0 次相交**。角色中心到建筑 mesh 的最小采样距离为 **4.724 米**；以 38 米地面、1.8 米身高、0.3 米半径的角色身体外包球保守估计，体表净距下界为 **3.464 米**。这两项不能写成同一种净距，也不是导航栅格的净距下界。审计只覆盖本次记录的插值轨迹及指定身体近似，不能排除记录间未观测的弯曲运动，也不能证明任意起终点都安全或必定到达。

运行证据保存在仓库根目录下被 Git 忽略的本地文件：[完整记录](../../artifacts/jev-free-city-run.json)、[到达截图](../../artifacts/jev-free-city-arrived.png)、[独立网格审计](../../artifacts/jev-free-city-mesh-audit.json)。这些文件没有随 Git 分发；模型依据结构化观测决策，截图用于界面验收，没有作为模型视觉输入。

旧版本交互验收曾覆盖：

- 屋顶和覆盖框外地面点击明确拒绝，原 URL 坐标保持；页面热更新刷新后恢复当前点对。
- 运行中拖动、滚轮缩放地图确实改变视图，不改变任务坐标或人物朝向。
- 交换 A/B 后坐标及 URL 互换，人物返回新起点，运行统计归零。
- 另一轮运行已行走 46.38 米、模型请求仍未返回时点击“回到起点”：人物返回 A，运行停止，决策、连续行动统计和规划任务清空；等待后位置未变，旧计划没有恢复执行。
- 在 **390 × 844** 手机布局中，选点提示与取消按钮可用，取消选点成功。

此前内置单楼任务的运行与审计也保留为研发归档，不混入当前扩区版本的数据。页面使用的是本地 Bridge SDK 执行通道，以上结果不代表独立 MCP stdio/HTTP 链路验收。

在仓库根目录可复查：

```powershell
rtk npm --prefix experiments/embodied-world-lab test
rtk npm --prefix experiments/embodied-world-lab run typecheck
rtk npm --prefix experiments/embodied-world-lab run build
```

旧山坡巡检和沿街原型的记录仍可作为研发归档，不作为当前自由任务版本的测试数据。

此前 Three.js 网格原型 `G:/code/jev-world-lab` 仍独立保留；这里继续使用 Cesium 与 cesium-mcp 的组合。
