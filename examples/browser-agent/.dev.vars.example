# Optional: token registered for the exact deployed origin in Chrome's WebMCP
# Origin Trial. The token is public metadata and enables WebMCP without local
# chrome://flags for visitors to that origin.
# WEBMCP_ORIGIN_TRIAL_TOKEN=replace-with-your-origin-trial-token
