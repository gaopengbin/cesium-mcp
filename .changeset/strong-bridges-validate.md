---
'cesium-mcp-contracts': minor
'cesium-mcp-bridge': patch
---

Add shared runtime input validation, per-command Bridge executor overrides, per-Viewer state isolation, and an idempotent Bridge lifecycle cleanup API.
