---
'cesium-mcp-contracts': minor
'cesium-mcp-runtime': minor
'cesium-mcp-webmcp': minor
---

Add session-scoped resource handles for storing GeoJSON and CZML once, then resolving `resourceId` across MCP, WebMCP, and the hosted function-calling agent.
